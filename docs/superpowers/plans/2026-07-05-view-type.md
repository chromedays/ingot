# ingot 뷰 타입 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `ingot` 라이브러리에 제네릭 비소유 뷰 타입 `view_t<T>`와 `view_*` 자유 함수를 추가한다.

**Architecture:** `view_t<T> {T* data; int64_t len}` — Odin/Zig 슬라이스와 EASTL 동적 extent span의 공통 패턴인 `{ptr, len}` 2워드 비소유 뷰. 요소 타입 `T`는 POD 필수(`static_assert`). 단일 동적 extent만 제공(fixed extent 특수화 없음). 팩토리는 `view_from` C++ 오버로드로 통일 — raw ptr+len, C 배열, `static_vector_t<T>` mutable/const. `string_t`와는 독립 타입으로 공존. 모든 코드는 기존 `ingot.h` 단일 파일에 추가(대부분 헤더 인라인 템플릿). TDD: `tests/test_view.cpp` 신규 TU.

**Tech Stack:** C++23, CMake, doctest v2.5.2 (vendored), CTest.

**참조 스펙:** [docs/superpowers/specs/2026-07-05-view-type-design.md](../specs/2026-07-05-view-type-design.md)

**커밋 정책:** AGENTS.md에 따라 커밋은 명시적 요청 시에만 수행. 각 Task의 커밋 단계는 계획의 일부지만, 실행 시 사용자가 승인할 때만 커밋한다.

---

## File Structure

| 파일 | 작업 | 책임 |
|---|---|---|
| `ingot.h` | Modify | `view_t<T>` 구조체 + `view_*` 자유 함수 템플릿 선언/인라인 정의 (`static_vector_t` 섹션 직후, `string_t` 섹션 이전) |
| `tests/test_view.cpp` | Create | `view_t<T>` + `view_*` doctest 테스트 |
| `tests/CMakeLists.txt` | Modify | `test_view` 실행 파일 추가 |

`ingot.cpp`는 수정하지 않는다 — `view_t<T>`와 모든 `view_*` 함수가 템플릿/짧은 접근자라 헤더에 인라인.

---

## Task 1: 테스트 스캐폴딩 (`test_view.cpp`)

뷰 테스트를 위한 별도 TU를 만들고 CMake에 등록한다. `doctest_main.cpp`는 이미 분리되어 있으므로(스트링 스펙 시점), 그대로 링크한다.

**Files:**
- Create: `tests/test_view.cpp`
- Modify: `tests/CMakeLists.txt`

- [ ] **Step 1: `tests/test_view.cpp` 스모크 케이스 생성**

```cpp
#include "doctest.h"

#include "ingot.h"

TEST_CASE("view types smoke") {
    CHECK(true);
}
```

- [ ] **Step 2: `tests/CMakeLists.txt` 수정**

기존 내용 끝에 `test_view` 실행 파일을 추가한다. 최종 파일 내용:

```cmake
add_executable(test_static_vector test_static_vector.cpp doctest_main.cpp)
target_link_libraries(test_static_vector PRIVATE ingot)
add_test(NAME test_static_vector COMMAND test_static_vector)

add_executable(test_string test_string.cpp doctest_main.cpp)
target_link_libraries(test_string PRIVATE ingot)
add_test(NAME test_string COMMAND test_string)

add_executable(test_view test_view.cpp doctest_main.cpp)
target_link_libraries(test_view PRIVATE ingot)
add_test(NAME test_view COMMAND test_view)
```

- [ ] **Step 3: 빌드 및 스모크 테스트 실행**

Run:
```bash
cmake --build build
ctest --test-dir build --output-on-failure -R test_view
```
Expected: `test_view` 빌드 성공, 1개 테스트 PASS (스모크).

- [ ] **Step 4: 커밋 (명시적 승인 시)**

```bash
git add tests/test_view.cpp tests/CMakeLists.txt
git commit -m "Add test_view scaffolding"
```

---

## Task 2: `view_t<T>` 구조체 + `view_from` 팩토리

뷰 타입의 핵심인 구조체와 모든 생성 경로를 `view_from` 오버로드로 구현한다.

**Files:**
- Modify: `ingot.h` (`static_vector_t` 섹션 끝 직후, `// === 스트링 타입 ===` 주석 이전에 삽입)
- Modify: `tests/test_view.cpp`

- [ ] **Step 1: 실패 테스트 작성 — `view_from(ptr, len)`과 C 배열**

`tests/test_view.cpp`의 스모크 `TEST_CASE` **다음에** 추가 (스모크 케이스는 유지):

```cpp
TEST_CASE("view_t: view_from ptr+len") {
    int buf[] = {10, 20, 30};
    ingot::view_t<int> v = ingot::view_from(buf, 3);
    CHECK_MESSAGE(ingot::view_len(v) == 3, "len should be 3");
    CHECK_MESSAGE(ingot::view_data(v) == buf, "data should point to source");
    CHECK_MESSAGE(!ingot::view_is_empty(v), "should not be empty");
}

TEST_CASE("view_t: view_from empty") {
    ingot::view_t<int> v = ingot::view_from(static_cast<int*>(nullptr), 0);
    CHECK_MESSAGE(ingot::view_is_empty(v), "empty view");
    CHECK_MESSAGE(ingot::view_len(v) == 0, "len 0");
}

TEST_CASE("view_t: view_from C array") {
    int arr[] = {1, 2, 3, 4};
    ingot::view_t<int> v = ingot::view_from(arr);
    CHECK_MESSAGE(ingot::view_len(v) == 4, "C array len 4");
    CHECK_MESSAGE(ingot::view_data(v) == arr, "points to array");
}
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

Run:
```bash
cmake --build build && ctest --test-dir build --output-on-failure -R test_view
```
Expected: 컴파일 에러 (`view_t` 미정의).

- [ ] **Step 3: `ingot.h`에 `view_t<T>` 구조체 + 관찰자 + 팩토리 추가**

`ingot.h`에서 `// === 스트링 타입 ===` 주석 **직전에** 삽입 (`static_vector_t` 관련 코드 뒤). 이 단계에서 구조체, `view_from(ptr,len)`, `view_from(arr)`, 그리고 테스트가 사용하는 관찰자(`view_len`/`view_is_empty`/`view_data`)를 함께 추가한다 (테스트 컴파일을 위해).

```cpp
// === 뷰 타입 ===

template<typename T>
struct view_t {
    static_assert(std::is_trivially_copyable_v<T> && std::is_standard_layout_v<T>,
                  "view_t<T> requires POD types");

    T*      data;
    int64_t len;
};
static_assert(std::is_trivially_copyable_v<view_t<int>> &&
              std::is_standard_layout_v<view_t<int>>,
              "view_t must be POD");

template<typename T>
view_t<T> view_from(T* data, int64_t len) {
    ingot_assert_(len >= 0, "view_from: negative length (len=%lld)",
                  static_cast<long long>(len));
    ingot_assert_(data != nullptr || len == 0,
                  "view_from: null data with non-zero length (len=%lld)",
                  static_cast<long long>(len));
    return view_t<T>{.data = data, .len = len};
}

template<typename T, int64_t N>
view_t<T> view_from(T (&arr)[N]) {
    return view_t<T>{.data = arr, .len = N};
}

template<typename T>
int64_t view_len(view_t<T> v) {
    return v.len;
}

template<typename T>
bool view_is_empty(view_t<T> v) {
    return v.len == 0;
}

template<typename T>
T* view_data(view_t<T> v) {
    return v.data;
}
```

- [ ] **Step 4: 테스트 실행 — 통과 확인**

Run:
```bash
cmake --build build && ctest --test-dir build --output-on-failure -R test_view
```
Expected: 4개 테스트 PASS (스모크 + ptr+len + empty + C array).

- [ ] **Step 5: 커밋 (명시적 승인 시)**

```bash
git add ingot.h tests/test_view.cpp
git commit -m "Add view_t struct with view_from factories and observers"
```

---

## Task 3: `view_from(static_vector_t<T>&)` 오버로드

`static_vector_t`에서 뷰를 추출하는 mutable/const 오버로드를 추가한다. const-정확성 자동 분기.

**Files:**
- Modify: `ingot.h` (Task 2에서 추가한 `view_from(arr)` 오버로드 다음에 추가)
- Modify: `tests/test_view.cpp`

- [ ] **Step 1: 실패 테스트 작성 — static_vector → view**

`tests/test_view.cpp` 끝에 추가. `static_vector_t` 생성을 위해 `heap_allocator_t` 사용 (기존 `test_static_vector.cpp` 패턴 참조).

```cpp
TEST_CASE("view_t: view_from static_vector mutable") {
    ingot::heap_allocator_t alloc;
    ingot::static_vector_t<int> sv;
    ingot::sv_create(sv, alloc, 4);
    ingot::sv_push(sv, 1);
    ingot::sv_push(sv, 2);
    ingot::sv_push(sv, 3);

    ingot::view_t<int> v = ingot::view_from(sv);
    CHECK_MESSAGE(ingot::view_len(v) == 3, "len matches sv count");
    CHECK_MESSAGE(ingot::view_data(v) == sv.data, "data points to sv buffer");

    // mutable: 요소 수정이 원본에 반영
    ingot::view_data(v)[0] = 99;
    CHECK_MESSAGE(sv.data[0] == 99, "mutation visible through source");

    ingot::sv_destroy(sv);
}

TEST_CASE("view_t: view_from static_vector const") {
    ingot::heap_allocator_t alloc;
    ingot::static_vector_t<int> sv;
    ingot::sv_create(sv, alloc, 2);
    ingot::sv_push(sv, 7);

    const ingot::static_vector_t<int>& csv = sv;
    ingot::view_t<const int> cv = ingot::view_from(csv);
    CHECK_MESSAGE(ingot::view_len(cv) == 1, "const view len");
    CHECK_MESSAGE(*ingot::view_data(cv) == 7, "const view reads element");

    ingot::sv_destroy(sv);
}
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

Run:
```bash
cmake --build build && ctest --test-dir build --output-on-failure -R test_view
```
Expected: 컴파일 에러 (`view_from(static_vector_t<T>&)` 오버로드 미정의; 모호한 호출 또는 존재하지 않음).

- [ ] **Step 3: `ingot.h`에 static_vector 오버로드 2종 추가**

Task 2에서 추가한 `view_from(T (&arr)[N])` 템플릿 **다음에** 추가:

```cpp
template<typename T>
view_t<T> view_from(static_vector_t<T>& sv) {
    return view_t<T>{.data = sv.data, .len = sv.count};
}

template<typename T>
view_t<const T> view_from(const static_vector_t<T>& sv) {
    return view_t<const T>{.data = sv.data, .len = sv.count};
}
```

- [ ] **Step 4: 테스트 실행 — 통과 확인**

Run:
```bash
cmake --build build && ctest --test-dir build --output-on-failure -R test_view
```
Expected: 6개 테스트 PASS (이전 4개 + mutable sv + const sv).

- [ ] **Step 5: 커밋 (명시적 승인 시)**

```bash
git add ingot.h tests/test_view.cpp
git commit -m "Add view_from overloads for static_vector_t (mutable/const)"
```

---

## Task 4: `view_at` (bounds-checked 접근)

인덱스 접근 자유 함수. 단일 템플릿이 `view_t<T>`(수정 가능)와 `view_t<const T>`(읽기 전용)를 모두 처리 — `T` 추론으로 반환 타입 결정.

**Files:**
- Modify: `ingot.h` (`view_data` 함수 다음에 추가)
- Modify: `tests/test_view.cpp`

- [ ] **Step 1: 실패 테스트 작성**

`tests/test_view.cpp` 끝에 추가:

```cpp
TEST_CASE("view_t: view_at") {
    int buf[] = {100, 200, 300};
    ingot::view_t<int> v = ingot::view_from(buf, 3);

    CHECK_MESSAGE(ingot::view_at(v, 0) == 100, "at 0");
    CHECK_MESSAGE(ingot::view_at(v, 2) == 300, "at last index");

    // mutable: 참조 반환으로 수정
    ingot::view_at(v, 1) = 250;
    CHECK_MESSAGE(buf[1] == 250, "view_at mutation visible in source");
}

TEST_CASE("view_t: view_at const view") {
    const int buf[] = {5, 10};
    ingot::view_t<const int> cv = ingot::view_from(buf, 2);
    CHECK_MESSAGE(ingot::view_at(cv, 0) == 5, "const view read at 0");
    CHECK_MESSAGE(ingot::view_at(cv, 1) == 10, "const view read at 1");
}
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

Run:
```bash
cmake --build build && ctest --test-dir build --output-on-failure -R test_view
```
Expected: 컴파일 에러 (`view_at` 미정의).

- [ ] **Step 3: `ingot.h`에 `view_at` 추가**

`view_data` 함수 템플릿 **다음에** 추가:

```cpp
template<typename T>
T& view_at(view_t<T> v, int64_t index) {
    ingot_assert_(index >= 0 && index < v.len,
                  "view_at: index out of range (index=%lld, len=%lld)",
                  static_cast<long long>(index),
                  static_cast<long long>(v.len));
    return v.data[index];
}
```

> 단일 템플릿으로 충분: `view_t<int>` → `T=int`, `int&` 반환(수정 가능); `view_t<const int>` → `T=const int`, `const int&` 반환(읽기 전용).

- [ ] **Step 4: 테스트 실행 — 통과 확인**

Run:
```bash
cmake --build build && ctest --test-dir build --output-on-failure -R test_view
```
Expected: 8개 테스트 PASS.

- [ ] **Step 5: 커밋 (명시적 승인 시)**

```bash
git add ingot.h tests/test_view.cpp
git commit -m "Add view_at with bounds checking"
```

---

## Task 5: `view_slice` (zero-copy 부분 뷰)

반개구간 `[low, high)` 슬라이싱.

**Files:**
- Modify: `ingot.h` (`view_at` 다음에 추가)
- Modify: `tests/test_view.cpp`

- [ ] **Step 1: 실패 테스트 작성**

`tests/test_view.cpp` 끝에 추가:

```cpp
TEST_CASE("view_t: view_slice") {
    int buf[] = {0, 10, 20, 30, 40, 50};
    ingot::view_t<int> v = ingot::view_from(buf, 6);

    ingot::view_t<int> mid = ingot::view_slice(v, 1, 4);
    CHECK_MESSAGE(ingot::view_len(mid) == 3, "slice len = high-low");
    CHECK_MESSAGE(ingot::view_data(mid) == buf + 1, "slice data offset");
    CHECK_MESSAGE(ingot::view_at(mid, 0) == 10, "slice[0]");
    CHECK_MESSAGE(ingot::view_at(mid, 2) == 30, "slice[last]");
}

TEST_CASE("view_t: view_slice edge cases") {
    int buf[] = {1, 2, 3};
    ingot::view_t<int> v = ingot::view_from(buf, 3);

    // 전체 슬라이스
    ingot::view_t<int> all = ingot::view_slice(v, 0, 3);
    CHECK_MESSAGE(ingot::view_len(all) == 3, "full slice");

    // 빈 슬라이스 (low == high)
    ingot::view_t<int> empty = ingot::view_slice(v, 2, 2);
    CHECK_MESSAGE(ingot::view_is_empty(empty), "empty slice");
    CHECK_MESSAGE(ingot::view_len(empty) == 0, "empty slice len 0");

    // 슬라이스의 슬라이스
    ingot::view_t<int> sub = ingot::view_slice(ingot::view_slice(v, 0, 3), 1, 2);
    CHECK_MESSAGE(ingot::view_len(sub) == 1, "nested slice");
    CHECK_MESSAGE(ingot::view_at(sub, 0) == 2, "nested slice value");
}
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

Run:
```bash
cmake --build build && ctest --test-dir build --output-on-failure -R test_view
```
Expected: 컴파일 에러 (`view_slice` 미정의).

- [ ] **Step 3: `ingot.h`에 `view_slice` 추가**

`view_at` 함수 템플릿 **다음에** 추가:

```cpp
template<typename T>
view_t<T> view_slice(view_t<T> v, int64_t low, int64_t high) {
    ingot_assert_(low >= 0 && high >= low && high <= v.len,
                  "view_slice: invalid range (low=%lld, high=%lld, len=%lld)",
                  static_cast<long long>(low),
                  static_cast<long long>(high),
                  static_cast<long long>(v.len));
    return view_t<T>{.data = v.data + low, .len = high - low};
}
```

> 단일 템플릿: `view_t<T>` → `view_t<T>` 반환, `view_t<const T>` → `view_t<const T>` 반환.

- [ ] **Step 4: 테스트 실행 — 통과 확인**

Run:
```bash
cmake --build build && ctest --test-dir build --output-on-failure -R test_view
```
Expected: 10개 테스트 PASS.

- [ ] **Step 5: 커밋 (명시적 승인 시)**

```bash
git add ingot.h tests/test_view.cpp
git commit -m "Add view_slice for zero-copy subviews"
```

---

## Task 6: 반복 (`view_begin`/`view_end` + ADL `begin`/`end`)

포인터 기반 반복자와 range-for 지원 ADL 함수. `static_vector_t`/`string_t`의 기존 `begin`/`end` 오버로드와 다른 파라미터 타입이라 모호하지 않음.

**Files:**
- Modify: `ingot.h` (`view_slice` 다음에 추가)
- Modify: `tests/test_view.cpp`

- [ ] **Step 1: 실패 테스트 작성**

`tests/test_view.cpp` 끝에 추가:

```cpp
TEST_CASE("view_t: pointer iteration") {
    int buf[] = {1, 2, 3};
    ingot::view_t<int> v = ingot::view_from(buf, 3);

    int sum = 0;
    for (int* p = ingot::view_begin(v); p != ingot::view_end(v); ++p) {
        sum += *p;
    }
    CHECK_MESSAGE(sum == 6, "manual pointer iteration sum");
}

TEST_CASE("view_t: range-for via ADL") {
    int buf[] = {5, 10, 15};
    ingot::view_t<int> v = ingot::view_from(buf, 3);

    int product = 1;
    for (int& x : v) {
        product *= x;
    }
    CHECK_MESSAGE(product == 750, "range-for product");

    // range-for 내 수정
    for (int& x : v) {
        x += 1;
    }
    CHECK_MESSAGE(buf[0] == 6, "range-for mutation visible");
    CHECK_MESSAGE(buf[2] == 16, "range-for mutation last");
}

TEST_CASE("view_t: range-for const view") {
    const int buf[] = {2, 4, 6};
    ingot::view_t<const int> cv = ingot::view_from(buf, 3);

    int total = 0;
    for (const int& x : cv) {
        total += x;
    }
    CHECK_MESSAGE(total == 12, "const view range-for");
}
```

- [ ] **Step 2: 테스트 실행 — 실패 확인**

Run:
```bash
cmake --build build && ctest --test-dir build --output-on-failure -R test_view
```
Expected: 컴파일 에러 (`view_begin`/`begin(view_t<T>)` 미정의; range-for가 `begin`/`end`를 찾지 못함).

- [ ] **Step 3: `ingot.h`에 반복 함수 추가**

`view_slice` 함수 템플릿 **다음에** 추가:

```cpp
template<typename T>
T* view_begin(view_t<T> v) {
    return v.data;
}

template<typename T>
T* view_end(view_t<T> v) {
    return v.data + v.len;
}

template<typename T>
T* begin(view_t<T> v) {
    return view_begin(v);
}

template<typename T>
T* end(view_t<T> v) {
    return view_end(v);
}
```

> `begin`/`end`는 `namespace ingot`의 자유 함수. range-for는 ADL로 인자 타입(`view_t<T>`)의 네임스페이스에서 후보를 찾는다. 기존 `begin(static_vector_t<T>&)` / `begin(string_t)` 오버로드와는 파라미터 타입이 달라 모호하지 않음.

- [ ] **Step 4: 테스트 실행 — 통과 확인**

Run:
```bash
cmake --build build && ctest --test-dir build --output-on-failure -R test_view
```
Expected: 13개 테스트 PASS.

- [ ] **Step 5: 커밋 (명시적 승인 시)**

```bash
git add ingot.h tests/test_view.cpp
git commit -m "Add view_begin/view_end and ADL begin/end for range-for"
```

---

## Task 7: 전체 검증

모든 타입의 테스트가 함께 통과하는지, 빌드가 깨끗한지 확인한다.

**Files:** (수정 없음 — 검증만)

- [ ] **Step 1: 전체 빌드 (clean)**

Run:
```bash
cmake --build build
```
Expected: 빌드 성공, 경고 없음 (`test_static_vector`, `test_string`, `test_view` 모두).

- [ ] **Step 2: 전체 테스트 실행**

Run:
```bash
ctest --test-dir build --output-on-failure
```
Expected: 3개 테스트 실행 파일 모두 PASS (`test_static_vector`, `test_string`, `test_view`). `test_view`는 13개 `TEST_CASE` PASS.

- [ ] **Step 3: POD `static_assert` 간접 확인**

`view_t`의 POD 검증은 이미 `ingot.h` 내 `static_assert(std::is_trivially_copyable_v<view_t<int>> && std::is_standard_layout_v<view_t<int>>, ...)` 로 컴파일 타임에 강제. 빌드 성공이 곧 검증 통과. 음성 컴파일 검증(POD가 아닌 `T` 거부)은 별도 프로브 없이 스펙 §7.3에 문서화된 대로 생략.

- [ ] **Step 4: `ingot.h` 섹션 순서 확인**

`ingot.h`에서 섹션 순서가 다음과 같은지 확인:
1. allocators (`allocator_t`, `heap_allocator_t`, `arena_allocator_t`)
2. `static_vector_t<T>` + `sv_*`
3. **`view_t<T>` + `view_*`** (신규)
4. `string_t` + `str_*` / `sb_*` / `ssb_*` / `utf8_*`

순서가 다르면 읽기 흐름을 위해 `view_t` 섹션을 `string_t` 섹션 직전으로 이동.

- [ ] **Step 5: 최종 커밋 (명시적 승인 시, 변경 사항 있는 경우만)**

```bash
git status
# 변경 사항이 있으면:
git add -A
git commit -m "Finalize view_t implementation"
```

---

## Self-Review 결과

**스펙 커버리지:**
- §2 타입 개요 → Task 2 (구조체)
- §3.1 레이아웃 → Task 2 (POD static_assert 포함)
- §3.2 연산 — 팩토리 → Task 2, 3 / 검사 → Task 2 / 접근 → Task 4 / 슬라이스 → Task 5 / 반복 → Task 6
- §4 `string_t` 관계 → 구현 불필요 (독립 유지 정책만 반영, 상호운용은 문서화만)
- §5 에러 처리 → 각 함수의 `ingot_assert_` (Task 2-6에 포함)
- §6 불변 조건 → POD static_assert (Task 2), 비소유 의미론 (설계 자체)
- §7 테스팅 → Task 1-6의 테스트 + Task 7 검증
- §8 파일 구성 → Task 1-6 (`ingot.h` + `test_view.cpp`)

**플레이스홀더 스캔:** TBD/TODO 없음. 모든 코드 블록은 완전한 구현 포함.

**타입 일관성:** `view_t<T>`, `view_from`, `view_len`, `view_is_empty`, `view_data`, `view_at`, `view_slice`, `view_begin`, `view_end`, `begin`, `end` — 모든 Task에서 일관된 시그니처. 필드명 `.data`/`.len` 일관. 접두사 `view_` 일관.
